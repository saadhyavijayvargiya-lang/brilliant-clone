{
  "name": "pathwise",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "firebase:deploy:rules": "firebase deploy --only firestore:rules",
    "firebase:deploy": "npm run build && firebase deploy --only hosting,functions,firestore:rules",
    "functions:install": "npm install --prefix functions",
    "functions:build": "npm run build --prefix functions"
  },
  "dependencies": {
    "firebase": "^11.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "firebase-tools": "^15.22.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
