{
  "name": "pathwise-tutor-worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put OPENAI_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
